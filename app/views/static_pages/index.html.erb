<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>Gossip Project</title>
  </head>
  <body>
    <div class="hero-image">
      <div class="hero-text">
        <h1>Bienvenue</h1>
        <h3>Sur Eventbrite Lyon</h3>
        <% if user_signed_in? %>
          <a href="<%= event_index_path %>" class="btn btn-outline-light">Get Started</a>
        <%else%>
          <div class="row">
            <%= link_to "Sign in", new_user_session_path, class: 'btn btn-outline-light mr-2' %>
            <%= link_to "Sign up", new_user_registration_path, class: 'btn btn-outline-light ml-2'%>
          </div>
        <% end %>
      </div>
    </div>
  </body
</html>
