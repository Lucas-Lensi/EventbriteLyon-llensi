<!DOCTYPE html>
<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <section class="jumbotron text-center">
      <div class="container">
        <h1 class="jumbotron-heading">All Event</h1>
        <p class="lead text-muted">Voici tous les évènements de Lyon, n'hésite pas a les parcourir et a participer a ceux qui te plaisent.</p>
        <p>
          <a href="<%= new_event_path %>" class="btn btn-primary my-2">Creer un evenement</a>
          <!-- <a href="#" class="btn btn-secondary my-2">Secondary action</a> -->
        </p>
      </div>
    </section>

    <div class="album py-5 bg-light">
      <div class="container">

        <div class="row">
          <% @events.each do |event| %>
          <div class="col-md-4">
            <div class="card mb-4 box-shadow">
              <img class="card-img-top" <%= image_tag "concert.jpg"%>
              <div class="card-body">
                <p class="card-title"><%= event.title %></p>
                <p class="card-text"><%= event.description %></p>
                <div class="d-flex justify-content-between align-items-center">
                  <div class="btn-group">
                    <a href="<%= event_path(event.id) %>"><button type="button" class="btn btn-sm btn-outline-secondary">Show</button></a>
                    <a href="<%= event_path(event.id) %>"><button type="button" class="btn btn-sm btn-outline-secondary">Show</button></a>
                  </div>
                  <small class="text-muted"><%= event.start_date %></small>
                </div>
              </div>
            </div>
          </div>
          <% end %>
        </div>
      </div>
    </div>

  </body>
</html>
